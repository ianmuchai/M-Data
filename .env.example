PORT=4000
# Leave CORS_ORIGIN blank during local development so Vite fallback ports like 5174 work.
# Set this to your production frontend origin for deployed tenants.
CORS_ORIGIN=
VITE_API_BASE_URL=
